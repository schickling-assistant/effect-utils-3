# Cookbook — Partial trees

Rendering a subtree of a Notion page (a single section, a toggle's children,
a callout body) without owning the whole page. Covers root selection,
boundary keys, and what the reconciler does at the edges of a partial mount.

<!-- TODO: include a "render just today's section" example grounded in pixeltrail's usage -->
<!-- TODO: call out pitfalls with orphaned keys at the boundary -->
